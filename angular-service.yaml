apiVersion: v1
kind: Service
metadata:
  name: carstore-frontend-service
spec:
  type: LoadBalancer  # Exposes the service externally using a cloud provider's load balancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: carstore-frontend
